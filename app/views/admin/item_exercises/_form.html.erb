<%= form_with(model: [@exercise, item_exercise]) do |form| %>
  <% if item_exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_exercise.errors.count, "error") %> prohibited this item_exercise from being saved:</h2>
      <ul>
        <% item_exercise.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <div class="field">
    <%= form.label :guide %>
    <%= form.text_field :guide %>
  </div>
  <%# <div class="field"> %>
  <%# <%= form.label :exercise_id %>
  <%# <%= form.select :exercise. %>
  <%# </div> %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
